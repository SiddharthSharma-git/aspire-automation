# Environment Configuration
# Copy this file to .env and populate the values.
# Never commit .env — it is already listed in .gitignore.

# Target environment: dev | qa | uat | staging | prod
ENV=

# Base URL of the Aspire application under test
BASE_URL=

# Test user credentials — store only in .env, never in source
TEST_USERNAME=
TEST_PASSWORD=
